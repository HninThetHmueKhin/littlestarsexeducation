<UserControl x:Class="ChildSafeSexEducation.Desktop.UserManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <UserControl.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#27AE60"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="User Management" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="#2C3E50"/>
            <TextBlock Text="View and manage registered users" 
                       FontSize="14" 
                       Foreground="#7F8C8D" 
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- User List -->
        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="8" 
                BorderBrush="#E9ECEF" 
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Search and Actions -->
                <StackPanel Grid.Row="0" 
                            Orientation="Horizontal" 
                            Margin="20,20,20,10">
                    <TextBox x:Name="SearchTextBox" 
                             Width="300" 
                             Height="35" 
                             Padding="10,8" 
                             FontSize="14" 
                             BorderBrush="#BDC3C7" 
                             BorderThickness="1" 
                             VerticalContentAlignment="Center"
                             TextChanged="SearchTextBox_TextChanged">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="Search users..." 
                                                                   Foreground="#95A5A6" 
                                                                   FontSize="14" 
                                                                   Margin="10,0,0,0"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    
                    <Button x:Name="RefreshButton" 
                            Content="🔄 Refresh" 
                            Style="{StaticResource ModernButton}"
                            Click="RefreshButton_Click"
                            Margin="10,0,0,0"/>
                </StackPanel>

                <!-- User Data Grid -->
                <DataGrid x:Name="UsersDataGrid" 
                          Grid.Row="1" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          CanUserDeleteRows="False" 
                          IsReadOnly="True" 
                          GridLinesVisibility="Horizontal" 
                          HeadersVisibility="Column" 
                          Margin="20,0,20,20">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" 
                                           Binding="{Binding Id}" 
                                           Width="50"/>
                        <DataGridTextColumn Header="Name" 
                                           Binding="{Binding Name}" 
                                           Width="150"/>
                        <DataGridTextColumn Header="Username" 
                                           Binding="{Binding Username}" 
                                           Width="120"/>
                        <DataGridTextColumn Header="Age" 
                                           Binding="{Binding Age}" 
                                           Width="60"/>
                        <DataGridTextColumn Header="Language" 
                                           Binding="{Binding PreferredLanguage}" 
                                           Width="100"/>
                        <DataGridTextColumn Header="Parent Email" 
                                           Binding="{Binding ParentEmail}" 
                                           Width="200"/>
                        <DataGridTextColumn Header="Registered" 
                                           Binding="{Binding CreatedAt, StringFormat='{}{0:MM/dd/yyyy}'}" 
                                           Width="100"/>
                        <DataGridTemplateColumn Header="Actions" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="👁️" 
                                                Width="30" 
                                                Height="30" 
                                                FontSize="12" 
                                                Background="#3498DB" 
                                                Foreground="White" 
                                                BorderThickness="0" 
                                                Margin="2"
                                                Click="ViewUserButton_Click"
                                                Tag="{Binding}"/>
                                        <Button Content="🗑️" 
                                                Width="30" 
                                                Height="30" 
                                                FontSize="12" 
                                                Background="#E74C3C" 
                                                Foreground="White" 
                                                BorderThickness="0" 
                                                Margin="2"
                                                Click="DeleteUserButton_Click"
                                                Tag="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
